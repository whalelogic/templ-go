package components 

import (
  "github.com/whalerapi/templ-go/models"
)

var post []models.Post

templ PostList() {
  
  data, err := os.ReadFile("allposts.json")
  if err != nil {
    fmt.Printf("Error: %s", err)
  }
  
}
